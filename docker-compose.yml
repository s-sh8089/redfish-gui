services:
  nginx:
    build:
      context: ./gui
    ports:
      - "3008:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - redfish-net

networks:
  redfish-net:
    external: true
